MASTER.lua is the main script
CLIENT.lua is the Gui script on the client
buildInfo.lua is a build information module which is bundled with each release and used for auto-updates. The first major chunk of code in the MASTER.lua file requires and runs updates before the main script starts. 

Other documents are just documentation and stuff.
